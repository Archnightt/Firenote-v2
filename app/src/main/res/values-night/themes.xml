<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Notepad" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- MAIN COLOR ACCENT -->
        <item name="colorPrimary">@color/lightorange</item>

        <!-- BACKGROUND COLOR-->
        <item name="colorPrimaryVariant">@color/lightgrey</item>

        <!-- MAIN FONT COLOR-->
        <item name="colorOnPrimary">@color/white</item>

        <!-- FONT COLOR -->
        <item name="colorOnBackground">@color/ioswhiteBG</item>

        <!-- CONTRAST FONT COLOR-->
        <item name="colorSecondary">@color/lightorange</item>

        <!-- BACKGROUND NOTES-->
        <item name="colorSecondaryVariant">@color/iosblack</item>

        <!-- STATUS BAR -->
        <item name="colorOnSecondary">@color/black</item>

        <!-- BUTTON COLOR -->
        <item name ="colorButtonNormal">@color/light</item>

        <item name = "colorOnTertiary">@color/iosgrey</item>
        <item name = "colorOnPrimaryContainer">@color/darkstroke</item>


        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnSecondary</item>
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetStyleWrapper</item>
    </style>

    <style name="bottomSheetStyleWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>


</resources>